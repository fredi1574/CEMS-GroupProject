package entity;

public class ConcreteUser extends User {
    public ConcreteUser(String id, String firstName, String lastName, String userName, String password, String email) {
        super(id, firstName, lastName, userName, password, email);
    }
}

